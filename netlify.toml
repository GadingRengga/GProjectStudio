# Netlify config for the ERP SPA (docs/CONFIG.md)

[build]
  command = "npm run build"
  publish = "dist"

# Required for Vue Router history mode — routes all paths to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
